# Copyright 2026 Canonical Ltd.
# See LICENSE file for licensing details.
command:
  'airflow kubernetes executor active':
    exit-status: 0
    exec: juju wait-for application --model "${model_name}" airflow-kubernetes-executor --query='name == "airflow-kubernetes-executor" && status == "active"'